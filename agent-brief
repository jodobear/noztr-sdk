#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

printf '# Agent Brief\n\n'
printf 'Current execution snapshot for AI agents.\n\n'
printf '## Repository\n\n'
printf -- '- root: `%s`\n' "$script_dir"
printf -- '- git repo: `%s`\n' "$(if [ -d "$script_dir/.git" ]; then echo yes; else echo no; fi)"

printf '\n## Read Now\n\n'
printf -- '- `AGENTS.md`\n'
printf -- '- `handoff.md`\n'
printf -- '- `docs/index.md`\n'
printf -- '- `docs/plans/build-plan.md`\n'
printf -- '- `docs/plans/implementation-quality-gate.md`\n'
printf -- '- `docs/plans/implemented-nips-applesauce-audit-2026-03-15.md`\n'
printf -- '- `docs/plans/implemented-nips-zig-native-audit-2026-03-15.md`\n'
printf -- '- `docs/plans/noztr-feedback-log.md`\n'

printf '\n## Load On Demand\n\n'
printf -- '- `docs/guides/TIGER_STYLE.md`, `docs/guides/NOZTR_SDK_STYLE.md`, `docs/guides/zig-patterns.md`, and `docs/guides/zig-anti-patterns.md` for implementation/review work\n'
printf -- '- `docs/guides/PROCESS_CONTROL.md` and `docs/plans/docs-surface-audit.md` when the task touches process refinement, control-doc cleanup, or docs-surface coherence\n'
printf -- '- `docs/plans/audit-lanes.md` when defining, revising, or selecting audit and review lanes\n'
printf -- '- `docs/research/` for SDK research refresh\n'
printf -- '- `docs/nips/` for protocol references\n'
printf -- '- when a task touches `noztr` directly, use `/workspace/projects/noztr/docs/release/README.md` and `/workspace/projects/noztr/examples/README.md` as the canonical public route\n'
printf -- '- `docs/plans/examples-tree-plan.md` and `examples/README.md` when the task touches examples or teaching posture\n'
printf -- '- `docs/plans/noztr-sdk-ownership-matrix.md` for boundary decisions\n'
printf -- '- `docs/plans/sdk-kickoff.md`, `docs/plans/package-layout-plan.md`, `docs/plans/noztr-integration-plan.md`, `docs/plans/testing-parity-strategy.md`, `docs/plans/api-ownership-map.md`, and `docs/plans/research-refresh-2026-03-14.md` for deeper planning background\n'
printf -- '- `docs/index.md` plus `handoff.md` name the workflow packet chain to load for the current slice; do not preload every packet family\n'
printf -- '- `examples/README.md` when the task touches examples, teaching posture, or agent discoverability\n'
printf -- '- `docs/archive/` for historical loops, superseded packets, and bootstrap context only when historical context is needed\n'
printf -- '- audit lesson: before new NIP work, look for proof obligations, seam-contract audits, state-machine tables, adversarial test matrices, and targeted audit findings in the active planning packet\n'
