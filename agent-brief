#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

printf '# Agent Brief\n\n'
printf 'Current execution snapshot for AI agents.\n\n'
printf '## Repository\n\n'
printf -- '- root: `%s`\n' "$script_dir"
printf -- '- git repo: `%s`\n' "$(if [ -d "$script_dir/.git" ]; then echo yes; else echo no; fi)"

printf '\n## Read Now\n\n'
printf -- '- `AGENTS.md`\n'
printf -- '- `handoff.md`\n'
printf -- '- `docs/plans/build-plan.md`\n'
printf -- '- `docs/plans/implementation-quality-gate.md`\n'
printf -- '- `docs/plans/sdk-kickoff.md`\n'
printf -- '- `docs/plans/package-layout-plan.md`\n'
printf -- '- `docs/plans/noztr-integration-plan.md`\n'
printf -- '- `docs/plans/testing-parity-strategy.md`\n'
printf -- '- `docs/plans/api-ownership-map.md`\n'
printf -- '- `docs/plans/research-refresh-2026-03-14.md`\n'
printf -- '- `docs/plans/autonomous-loop-phases-1-4.md`\n'
printf -- '- `docs/plans/noztr-sdk-ownership-matrix.md`\n'

printf '\n## Load On Demand\n\n'
printf -- '- `docs/guides/TIGER_STYLE.md`, `docs/guides/NOZTR_SDK_STYLE.md`, `docs/guides/zig-patterns.md`, and `docs/guides/zig-anti-patterns.md` for implementation/review work\n'
printf -- '- `docs/research/` for SDK research refresh\n'
printf -- '- `docs/nips/` for protocol references\n'
printf -- '- `docs/plans/noztr-sdk-seeding-prompt.md` as historical bootstrap context\n'
printf -- '- `docs/plans/implementation-quality-gate.md` for new NIP/workflow execution discipline\n'
